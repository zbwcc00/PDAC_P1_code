# Copy this file to config/project_paths.R and edit the path for your computer.
# The local file is ignored by Git.

PDAC_PROJECT_ROOT <- "D:/path/to/PDAC_P1"
RAW_DATA_ROOT <- file.path(PDAC_PROJECT_ROOT, "data")
EXTERNAL_DATA_ROOT <- file.path(PDAC_PROJECT_ROOT, "data_external")
ANALYSIS_ROOT <- file.path(PDAC_PROJECT_ROOT, "analysis")
RESULTS_ROOT <- file.path(PDAC_PROJECT_ROOT, "results")
